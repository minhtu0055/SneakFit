@model SneakFit.ViewModels.Catalog.DeGiay.ThemDeGiay

<form asp-controller="DeGiay" asp-action="Create" id="createDeGiayForm" method="post" data-confirm="Bạn có chắc chắn muốn làm điều đó không?">
    <div class="mb-3">
        <label asp-for="TenDeGiay" class="form-label">Tên đế giày</label>
        <input asp-for="TenDeGiay" class="form-control" placeholder="Nhập tên đế giày"
               pattern="^[a-zA-ZÀ-ỹ\s]+$" 
               oninput="validateInput(this)"
               maxlength="50"
               title="Vui lòng chỉ nhập chữ cái"></input>
        <span asp-validation-for="TenDeGiay" class="text-danger"></span>
        <span id="customError" class="text-danger" style="display: none;">Vui lòng chỉ nhập chữ cái</span>
    </div>
    <div class="text-end">
        <button type="submit" class="btn btn-primary">Thêm</button>
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
    </div>
</form>

<script>
function validateInput(input) {
    const pattern = /^[a-zA-ZÀ-ỹ\s]+$/;
    const errorSpan = document.getElementById('customError');
    
    if (!pattern.test(input.value) && input.value !== '') {
        errorSpan.style.display = 'block';
        input.setCustomValidity('Vui lòng chỉ nhập chữ cái');
    } else {
        errorSpan.style.display = 'none';
        input.setCustomValidity('');
    }
}
</script>